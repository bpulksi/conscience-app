<!--
  ADD THESE TO android/app/src/main/AndroidManifest.xml
  inside the <manifest> tag (permissions) and <application> tag (service/activity)
-->

<!-- ===== PERMISSIONS (inside <manifest>) ===== -->
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
<uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.INTERNET" />


<!-- ===== ACCESSIBILITY SERVICE (inside <application>) ===== -->
<service
    android:name=".ConscienceAccessibilityService"
    android:exported="true"
    android:label="Conscience Screen Monitor"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>


<!-- ===== BLOCK ACTIVITY (inside <application>) ===== -->
<activity
    android:name=".BlockActivity"
    android:excludeFromRecents="true"
    android:exported="false"
    android:showOnLockScreen="true"
    android:taskAffinity=""
    android:theme="@style/Theme.Translucent" />
