<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch theme — shown while app boots -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowBackground">@color/app_bg</item>
    </style>

    <!-- Normal app theme — used after Flutter takes over -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowBackground">@color/app_bg</item>
    </style>

    <!-- Translucent theme for BlockActivity overlay -->
    <style name="Theme.Translucent" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowShowWallpaper">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- Material Button style for BlockActivity -->
    <style name="Widget.MaterialComponents.Button.OutlinedButton" parent="@android:style/Widget.Button">
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
